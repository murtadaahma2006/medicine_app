<?xml version="1.0" encoding="utf-8"?>
<!-- ويدجت الشاشة الرئيسية — منصة الطب الباطني.
     حجم افتراضي 4x2 — قابل للتحجيم (minWidth/minHeight متوافقان
     مع شبكة الأيقونات في أندرويد 12+). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/widget_background"
    android:clickable="true"
    android:focusable="true">

    <!-- ── الرأس: العنوان ── -->
    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_title"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="@color/widget_text_secondary" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="6dp"
        android:gravity="center_vertical">

        <!-- ── الرقم الكبير: البطاقات المستحقة ── -->
        <TextView
            android:id="@+id/widget_due_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textSize="44sp"
            android:textStyle="bold"
            android:textColor="@color/widget_due_normal"
            android:fontFamily="sans-serif-condensed" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/widget_due_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_due_label"
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="@color/widget_text_primary" />

            <!-- ── الإنجاز اليومي ── -->
            <ProgressBar
                android:id="@+id/widget_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="8dp"
                android:layout_marginTop="6dp"
                android:max="100"
                android:progress="0"
                android:progressTint="@color/widget_accent"
                android:progressBackgroundTint="@color/widget_track" />

            <TextView
                android:id="@+id/widget_progress_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="3dp"
                android:text="@string/widget_progress_placeholder"
                android:textSize="11sp"
                android:textColor="@color/widget_text_secondary" />
        </LinearLayout>
    </LinearLayout>

    <!-- ── المعلومة الطبية السريعة ── -->
    <TextView
        android:id="@+id/widget_tip"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="10dp"
        android:text="@string/widget_tip_placeholder"
        android:textSize="12sp"
        android:lineSpacingExtra="2dp"
        android:maxLines="3"
        android:ellipsize="end"
        android:textColor="@color/widget_text_secondary" />
</LinearLayout>
